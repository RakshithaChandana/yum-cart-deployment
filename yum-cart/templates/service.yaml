apiVersion: v1
kind: Service
metadata:
  name: {{ include "yum-cart.fullname" . }}
  labels:
    {{- include "yum-cart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "yum-cart.selectorLabels" . | nindent 4 }}
